apiVersion: v1
kind: Pod
metadata:
  name: "{{ "{{" }} include "{{ cookiecutter.service_slug }}.fullname" . {{ "}}" }}-test-connection"
  labels:
    {{ "{{" }}- include "{{ cookiecutter.service_slug }}.labels" . | nindent 4 {{ "}}" }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ "{{" }} include "{{ cookiecutter.service_slug }}.fullname" . {{ "}}" }}:{{ "{{" }} .Values.service.port {{ "}}" }}']
  restartPolicy: Never
